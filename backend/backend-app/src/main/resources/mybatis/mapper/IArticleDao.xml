<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.idealer01.infrastructure.dao.IArticleDao">

    <resultMap id="dataMap" type="cn.idealer01.infrastructure.dao.po.Article">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="summary" property="summary"/>
        <result column="theme" property="theme"/>
        <result column="cover_image" property="coverImage"/>
        <result column="status" property="status"/>
        <result column="author_id" property="authorId"/>
        <result column="view_count" property="viewCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryById" parameterType="java.lang.Long" resultMap="dataMap">
        select * from article where id = #{id}
    </select>

    <select id="queryList" resultMap="dataMap">
        select *
        from article
        <where>
            <if test="keyword != null and keyword != ''">
                and title like concat('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
        limit #{offset}, #{limit}
    </select>

    <select id="queryCount" resultType="long">
        select count(*)
        from article
        <where>
            <if test="keyword != null and keyword != ''">
                and title like concat('%', #{keyword}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
    </select>

    <select id="queryByTheme" parameterType="java.lang.String" resultMap="dataMap">
        select *
        from article
        where theme = #{theme}
          and status = 'PUBLISHED'
        order by create_time desc
    </select>

    <insert id="insert" parameterType="cn.idealer01.infrastructure.dao.po.Article" useGeneratedKeys="true" keyProperty="id">
        insert into article(title, content, summary, theme, cover_image, status, author_id, view_count)
        values (#{title}, #{content}, #{summary}, #{theme}, #{coverImage}, #{status}, #{authorId}, #{viewCount})
    </insert>

    <update id="update" parameterType="cn.idealer01.infrastructure.dao.po.Article">
        update article
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="summary != null">summary = #{summary},</if>
            <if test="theme != null">theme = #{theme},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="status != null">status = #{status},</if>
            update_time = CURRENT_TIMESTAMP
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Long">
        delete from article where id = #{id}
    </delete>

    <update id="incrementViewCount" parameterType="java.lang.Long">
        update article set view_count = view_count + 1 where id = #{id}
    </update>

</mapper>
