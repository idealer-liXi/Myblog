<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="cn.idealer01.infrastructure.dao.IUserDao">

    <resultMap id="dataMap" type="cn.idealer01.infrastructure.dao.po.User">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="level_id" property="levelId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>
    <insert id="insertUser" parameterType="cn.idealer01.infrastructure.dao.po.User">
        insert into user(username, password) values(#{username}, #{password})
    </insert>
    <select id="getUserByUserName" parameterType="java.lang.String" resultMap="dataMap">
        select username, password, level_id, status
        from user
        where username = #{username};
    </select>

</mapper>